<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}Devour It!{{else}}Reorder Da Burger!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Delete Burger</button>
</li>
